    let number = prompt('Please Enter a Number to Square:')

    function square(numberToSquare) {
      return numberToSquare * number;
    }
    alert(square(number));


    function math() {

      let x, y;
      x = prompt("Enter a number to multiply");
      y = prompt("Enter another number")
      let result = Number(x) * Number(y);
      alert("The result is" + ' ' + result);
    }
    square(x, y);
